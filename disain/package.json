{
  "main": "gulpfile.js",
  "devDependencies": {
    "eslint": "^8.57.1",
    "prettier": "^2.8.8",
    "browser-sync": "^2.27.11",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^4.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-imagemin": "^7.1.0",
    "gulp-npm-script-sync": "^1.1.0",
    "gulp-sass": "^3.1.0",
    "gulp-sourcemaps": "^2.6.0",
    "gulp-uglify": "^3.0.0",
    "pump": "^1.0.2"
  },
  "scripts": {
    "build:images": "gulp build:images",
    "build:favicon": "gulp build:favicon",
    "build:favicon_svg": "gulp build:favicon_svg",
    "build:fonts": "gulp build:fonts",
    "build:css": "gulp build:css",
    "build:js_main": "gulp build:js_main",
    "build:js_form": "gulp build:js_form",
    "build:js_legalperson": "gulp build:js_legalperson",
    "build": "gulp build",
    "format": "prettier --check ${TARGET:-.}",
    "format:fix": "prettier --write ${TARGET:-.}",
    "lint": "eslint ${TARGET:-scripts/}",
    "lint:fix": "eslint --fix ${TARGET:-scripts/}"
  }
}
