# Mark Lombok-generated code so coverage and static analysis tools can ignore it.
lombok.addLombokGeneratedAnnotation = true

# Copy Spring @Qualifier from fields to Lombok-generated constructor parameters.
# Needed when @RequiredArgsConstructor is used with multiple beans of the same type.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
